<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jorgeyampara.raicesnuevo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoapk"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logoapk"
        android:supportsRtl="true"
        android:theme="@style/Theme.Raices">
        <activity android:name=".ResetPassword"></activity>
        <activity android:name=".RegistrarseActivity" />
        <activity android:name=".LucesActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RiegoActivity" />
        <activity android:name=".ValoresActivity" />
        <activity android:name=".ContactosActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>